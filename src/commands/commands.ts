/**
 * The manifest requires a function file for the ribbon, even though every
 * Mailflow command opens the task pane rather than running headless. Keeping
 * it minimal leaves room for a future one-click action without a manifest
 * change.
 */
Office.onReady(() => {
  // Nothing to register yet.
});
